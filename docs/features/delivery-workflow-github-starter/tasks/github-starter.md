# GitHub Delivery Workflow Starter Tasks

1. Configure the GitHub Project and GitHub App.
2. Run `dw init` and review `.dw/config.yaml`.
3. Add and require the validation workflow check.
4. Add the merge-transition workflow.
5. Add the scheduled reconciliation workflow.
6. Test the four phase transitions in a staging Project.
