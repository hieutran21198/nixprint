# Agent Harness Write Boundaries Tasks

1. Replace free-text implementation boundaries with `writePaths` and
   `writeGlobs`.
2. Generate expert boundary instructions and native client guardrails.
3. Add the required Linux Bubblewrap runner.
4. Add configuration, asset, warning, and runtime boundary tests.
5. Update service and feature documentation.
